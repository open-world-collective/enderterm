"""EnderTerm package.

This repo historically shipped as a single script (`enderterm/nbttool.py`). We keep
that CLI entrypoint, but factor core logic into importable modules to make
testing and reuse easier.
"""
